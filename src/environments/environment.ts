import { NavigationSettings } from 'src/app/shared/files/type';

export const environment = {
  production: false,
  hmr: false
};

export const defaultNavigation: NavigationSettings = {
  toolbar: false,
  sidemenu: false
};

export const defaultTitle = 'AzzurroDigitale';
export const PATH = 'https://azzurrodigitale.com/';
